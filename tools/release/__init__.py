"""Repository-only release tooling (C1 §C1.1). NOT part of the runtime
package: it is excluded from ``[tool.setuptools] packages`` and from runtime
package claims. It orchestrates the pure evaluator over filesystem evidence."""
